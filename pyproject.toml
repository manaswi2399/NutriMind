[tool.black]
line-length = 88
target-version = ['py310']

[tool.isort]
profile = "black"

[tool.ruff]
line-length = 88
select = ["E", "F", "W", "I", "B", "C", "TID"]
exclude = ["node_modules", "build", "dist"]

[tool.mypy]
python_version = "3.10"
ignore_missing_imports = true
